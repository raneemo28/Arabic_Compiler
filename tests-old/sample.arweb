<صفحة>
    <عنوان>مرحبا</عنوان>
</صفحة>

لوحة {
    لون: احمر؛
    هامش: 10بكسل؛
}

ثابت اسم: سلسلة = "علي"؛
ثابت ارقام: عدد[] = [1، 2، 3]؛

<صفحة معرف="رئيسية">
    <عنوان>مرحبا</عنوان>
    <صورة مصدر="test.png"/>
</صفحة>

لوحة {
    لون-الخلفية: #FF0000؛
    عرض: تلقائي؛
}

// تم حذف التكرار هنا لكي يمر التحليل الدلالي بسلام

دالة مرحبا(اسم-المستخدم) {
    اطبع("مرحبا")؛
}

اذا (صحيح) {
    ثابت س: عدد = 5؛
}

// --- JsAssignmentStatementNode ---
// ملحوظة: بما انه "ثابت" لا يفضل برمجياً إعادة تعيينه، 
// ولكن لاختبار عقدة الإسناد (Assignment) سنستخدم "متغير" بدلاً من "ثابت"
متغير عداد: عدد = 0؛
عداد = 10؛

// --- JsForLoopNode ---
حلقة (ثابت ي: عدد = 0؛ ي؛ ي) {
    اطبع("حلقة")؛
}

// --- JsWhileLoopNode ---
طالما (صحيح) {
    اطبع("تكرار")؛
}

// --- JsTryCatchNode ---
حاول {
    اطبع("محاولة")؛
} امسك (خطا) {
    اطبع("خطا")؛
} اخيرا {
    اطبع("اخيرا")؛
}

// --- JsInterfaceDeclarationNode ---

/* hiiii */ 
واجهة مستخدم { }